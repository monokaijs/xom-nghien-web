import VoicePage from '@/components/voice/VoicePage';

export const metadata = {
  title: 'Voice Rooms | Xóm Nghiện',
  description: 'Tham gia phòng voice và chat trực tiếp cùng cộng đồng.',
};

export default function VoiceRoomsPage() {
  return <VoicePage />;
}
