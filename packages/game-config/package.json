{
  "name": "@xom/game-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@xom/tsconfig": "workspace:*",
    "typescript": "^5.9.3"
  }
}
